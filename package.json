{
  "name": "liquidai",
  "version": "1.0.0",
  "description": "AI-driven liquidity orchestration protocol for ETHOnline 2025",
  "main": "index.js",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js",
    "clean": "rm -rf node_modules cache artifacts frontend/node_modules frontend/.next"
  },
  "keywords": [
    "defi",
    "liquidity",
    "ai",
    "portfolio",
    "rebalancing",
    "ethereum",
    "pyth",
    "envio"
  ],
  "author": "LiquidAI Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.8.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}